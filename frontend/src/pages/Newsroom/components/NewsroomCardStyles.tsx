import newsroomCardStyles from "../newsroomCards.css?inline";

const NewsroomCardStyles = () => (
  <style data-newsroom-card-styles>{newsroomCardStyles}</style>
);

export default NewsroomCardStyles;
